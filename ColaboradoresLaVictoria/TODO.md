# TODO List for Fixing Notifications Button

- [x] Fix prop name in App.jsx: Change `onclose` to `onClose` in the Notificaciones component call.
- [x] Fix close button onClick in Notificaciones.jsx: Change `onClick={() => setMostarNotificaciones(true)}` to `onClick={onClose}`.
- [ ] Test the notifications slide functionality to ensure it works correctly.
